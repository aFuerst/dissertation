
We have implemented the keep-alive and the provisioning policies as part of our FaaSCache framework built on top of OpenWhisk v .
FaaSCache replaces the default OpenWhisk TTL-based keep-alive policy with the Greedy-Dual-Size-Frequency approach.

We maintain a 